// /** @type {import('prettier').Config & import('prettier-plugin-tailwindcss').options} */
import * as pptc from "prettier-plugin-tailwindcss";
const config = {
  // plugins: [pptc],
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: "all",
};

export default config;
